PORT=3001

# Frontend URL(s) allowed by CORS. Use comma-separated URLs for multiple frontends.
FRONTEND_URL=http://localhost:3001

# Public backend URL used by /config.js so the frontend knows where the API is.
BACKEND_URL=http://localhost:3001
# Optional direct override. Example: https://api.example.com/api
API_BASE_URL=

# MySQL database. You can use either DATABASE_URL or the separate DB_* values.
DATABASE_URL=
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=halqa4_roundu

JWT_SECRET=change_this_secret
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
